<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- gif.product.category tree view -->
    <record id="gif_product_category_view_tree" model="ir.ui.view">
        <field name="name">gif.product.category.view.tree</field>
        <field name="model">gif.product.category</field>
        <field name="arch" type="xml">
            <tree>
                <!-- Add your fields here -->
                <field name="name"/>
                <field name="gif_category_active"/>
            </tree>
        </field>
    </record>

    <!-- gif.product.category form view -->
    <record id="model_name_view_form" model="ir.ui.view">
        <field name="name">gif.product.category.view.form</field>
        <field name="model">gif.product.category</field>
        <field name="arch" type="xml">
            <form string="">
                <sheet>
                    <group>
                        <!-- Add your fields here -->
                        <field name="name"/>
                        <field name="gif_category_active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- gif.product.category action window -->
    <record id="gif_product_category_action" model="ir.actions.act_window">
        <field name="name">gif.product.category.action</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">gif.product.category</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Aún no hay categorias de producto.
            </p><p>
                Por favor cree categorias para su producto.
            </p>
        </field>
    </record>

    <menuitem id="gif_product_category_menu_act" name="Categorias de Productos" parent="stock.menu_stock_general_settings" action="gif_product_category_action" sequence="210"/>
</odoo>