<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- gif.product.brand tree view -->
    <record id="gif_product_brand_view_tree" model="ir.ui.view">
        <field name="name">gif.product.brand.view.tree</field>
        <field name="model">gif.product.brand</field>
        <field name="arch" type="xml">
            <tree>
                <!-- Add your fields here -->
                <field name="name"/>
                <field name="gif_brand_active"/>
            </tree>
        </field>
    </record>

    <!-- gif.product.brand form view -->
    <record id="model_name_view_form" model="ir.ui.view">
        <field name="name">gif.product.brand.view.form</field>
        <field name="model">gif.product.brand</field>
        <field name="arch" type="xml">
            <form string="">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="gif_brand_active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- gif.product.brand action window -->
    <record id="gif_product_brand_action" model="ir.actions.act_window">
        <field name="name">gif.product.brand.action</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">gif.product.brand</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Aún no hay marcas de producto.
            </p><p>
                Por favor cree marcas de producto. 
            </p>
        </field>
    </record>

    <menuitem id="gif_product_brand_menu_act" name="Categorias de Productos" parent="stock.menu_stock_general_settings" action="gif_product_brand_action" sequence="230"/>

</odoo>