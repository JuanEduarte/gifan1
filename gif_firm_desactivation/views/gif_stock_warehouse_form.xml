<?xml version='1.0' encoding='utf-8'?>
<odoo>
    
    <!-- stock.warehouse inherit form view -->
    <record id="view_id" model="ir.ui.view">
        <field name="name">stock.warehouse.inherit.view.form</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">

            <xpath expr="//page[@name='warehouse_config']/group" position="inside">
                <group name="control_pac" string="Control de timbrado">
                    <field name="gif_firm_flag"/>
                </group>
            </xpath>
            
        </field>
    </record>

</odoo>